org.gradle.configuration.cache=true

# this version doesn't matter, we just want to make sure that the plugin properly sets up the
# requested version of Yawn's processor
# TODO: don't require consumers to set this property. E.g. dynamically set this at publishing time
com.faire.yawn.version=0.1.4
